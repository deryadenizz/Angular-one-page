import { OnepagePipelineDirective } from './onepage-pipeline.directive';

describe('OnepagePipelineDirective', () => {
  it('should create an instance', () => {
    const directive = new OnepagePipelineDirective();
    expect(directive).toBeTruthy();
  });
});
