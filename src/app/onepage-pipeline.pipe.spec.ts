import { OnepagePipelinePipe } from './onepage-pipeline.pipe';

describe('OnepagePipelinePipe', () => {
  it('create an instance', () => {
    const pipe = new OnepagePipelinePipe();
    expect(pipe).toBeTruthy();
  });
});
